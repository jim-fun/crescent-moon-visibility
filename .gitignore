# Build artifacts (now centralized in bin/)
bin/
*.out
*.exe
*.o
crescent_maps

# Local release packages / staging (scripts/package.sh, make package, make dist)
dist/
crescent-moon-visibility-*.tar.gz
crescent-moon-visibility-*.zip
crescent-moon-visibility-*-amd64/
crescent-moon-visibility-*-arm64/

# Generated map output (raw RGBA bins / WEBP / per-run output dirs)
*.bin
*.webp
output_maps/
maps_*/
outmaps/
estimates/
test/

# Python
__pycache__/
*.pyc
.venv/

# Editor / tooling
.ipynb_checkpoints/
.claude/
.DS_Store

# NOTE: *.png is intentionally not globbed globally.
# The base map lives in data/map_nasa.png (tracked).
# All generated output PNGs live inside ignored directories (output_maps/, etc.).

**/.ipynb_checkpoints/

# === Internal AI / Agentic tooling (maintainer + Grok only) ===
# These are deliberately excluded when pushing to the public
# GitHub mirror via scripts/push-to-github.sh.
# They only exist in the internal Gitea repository.
# AGENTIC_WORKFLOW.md
# GITHUB_MIGRATION.md
# scripts/agentic-review.sh
# scripts/agents/
.agentic-review/

# Web UI runtime artifacts
web_errors.log
web_errors*.log

# Internal agentic / scratch directories (maintainer only)
.agentic/

# WordPress plugin build artifacts (reproducible via wordpress-plugin/scripts/build-plugin.sh)
wordpress-plugin/*.zip
wordpress-plugin/dist/

# Local pre-change snapshots
backups/
