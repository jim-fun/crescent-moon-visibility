# Claude Production Output — Integration Instructions

This folder is for the merged production implementation generated by Claude using the prompt:

`wordpress-plugin/prompts/claude-production-app-parity-interactive.md`

## When Claude finishes

Claude was instructed to output full file contents for the recommended production files.

### Expected Output Format
Claude should produce something like:

```
**File: wordpress-plugin/plugin/includes/interactive.php**
```php
... full code ...
```

**File: wordpress-plugin/plugin/public/interactive-renderer.php**
...
```

## How to apply the output

### Recommended Process

1. **Copy each file block** Claude generates.
2. **Create or overwrite** the corresponding file in the real plugin directory.
3. **Review** for any remaining TODOs or placeholders.
4. **Test** the shortcode `[crescent_visibility_interactive]` after importing data.

### Files You Should Expect

Claude was asked to produce (at minimum):

- `plugin/includes/interactive.php` (or equivalent) — AJAX + smart defaults
- `plugin/public/interactive-renderer.php` — The main shortcode renderer
- `plugin/assets/js/interactive.js` — Frontend logic
- Updates to `crescent-visibility.php` (main plugin loader)
- Possibly a CSS file
- Admin notices / documentation updates

## Current State (Parallel Work)

Some production-grade files have already been started manually in:

- `wordpress-plugin/plugin/includes/interactive.php`
- `wordpress-plugin/plugin/public/interactive-renderer.php`
- `wordpress-plugin/plugin/assets/js/interactive.js`
- `wordpress-plugin/plugin/assets/css/interactive.css`

You can use Claude's versions to **replace or improve** these.

## After Integration

Once the files are in place:

1. Re-import your real JSON data (important for `q_at_best` and `moon_age_at_best` columns).
2. Test the shortcode on a page: `[crescent_visibility_interactive]`
3. Verify AJAX calls work (browser dev tools → Network tab).
4. Check that defaults load Jerusalem + a sensible new moon.

## Need Help?

If Claude's output has issues or gaps, paste the relevant sections back here and I will help integrate / fix / complete the implementation.

---

**Status**: Waiting for Claude's full response to the production prompt.
